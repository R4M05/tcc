/** 
import { ComponentFixture, TestBed } from '@angular/core/testing';

import { AnestheticsInfoComponent } from './anesthetics-info.component';

describe('AnestheticsInfoComponent', () => {
  let component: AnestheticsInfoComponent;
  let fixture: ComponentFixture<AnestheticsInfoComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ AnestheticsInfoComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(AnestheticsInfoComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/
